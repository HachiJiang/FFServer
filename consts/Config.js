'use strict';

/*
 *
 * Configuration constants
 *
 * client side should keep the same as server side
 *
 */

const ID_SEPARATOR = ',';
const DATABASE = 'mockdb';

module.exports = {
    ID_SEPARATOR,
    DATABASE
};